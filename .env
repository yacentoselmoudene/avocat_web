# Configuration Firebase FCM
FIREBASE_CREDENTIALS=C:\Users\shama\PycharmProjects\Avocat\avocat-dc133-0a92dc1a0f9e.json
FIREBASE_PROJECT_ID=avocat-dc133

# Configuration Django de base
DJANGO_ENV=development
SECRET_KEY='django-insecure-)+t1wy%=dh*up@k_w7tiy%bxsdw!ffq*m!if(9+)uwno8!j!bw'
DEBUG=True

# Database (for both MySQL container and Django)
DB_NAME=cabinetavocat
DB_USER=avocat
DB_PASSWORD=avocatpass
DB_ROOT_PASSWORD=rootpass

# Frontend (Vite) config
# When running with docker compose, the frontend will reach the API at http://web:8000
# For running Vite locally without Docker, set this to http://localhost:8000
VITE_API_URL=http://localhost:8000/api

# Host port mappings (optional; change to avoid conflicts)
WEB_HOST_PORT=8000
FRONTEND_HOST_PORT=5173
PHPMYADMIN_HOST_PORT=8080
# To expose MySQL on the host, add a ports mapping in docker-compose.yml (e.g., 3307:3306)
# and optionally set DB_HOST_PORT=3307
#DB_HOST_PORT=3307
